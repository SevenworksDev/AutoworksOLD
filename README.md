# Autoworks - The best CommandBot for Geometry Dash Comments!
## This only supports Linux systems, This WILL NOT run on Windows!
# Setup (in only one line!)
Use this command:
```
bash <(curl -s https://pastebin.com/raw/MwaRaDyU)
```
# Run
Use this command:
```bash
python main.py
```
